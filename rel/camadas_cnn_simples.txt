Entrada: Imagem 32x32x3 (RGB)

Conv2d + ReLU + MaxPool:
Conv2d(3, 16, 3x3, padding=1) → 32x32x16
MaxPool2d(2) → 16x16x16

Conv2d + ReLU + MaxPool:
Conv2d(16, 32, 3x3, padding=1) → 16x16x32
MaxPool2d(2) → 8x8x32

Flatten: 8x8x32 → 2048

Linear + ReLU: 2048 → 128

Linear: 128 → 10 (classes)

COM CORREÇÃO DE OVERFITTING:
Entrada: Imagem 32x32x3 (RGB)

Conv2d + ReLU + Dropout + MaxPool:
Conv2d(3, 16, 3x3, padding=1) → 32x32x16
ReLU
Dropout(0.45)
MaxPool2d(2) → 16x16x16

Conv2d + ReLU + Dropout + MaxPool:
Conv2d(16, 32, 3x3, padding=1) → 16x16x32
ReLU
Dropout(0.45)
MaxPool2d(2) → 8x8x32

Flatten: 8x8x32 → 2048

Linear + ReLU + Dropout: 2048 → 128
ReLU
Dropout(0.45)

Linear: 128 → 10 (classes)
